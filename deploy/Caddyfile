app.shanaiai.com {
	reverse_proxy 127.0.0.1:8000
}

# shanaiai.com (bare + www) is the landing page — served by Vercel, not
# this server. DNS for those hostnames points at Vercel, so Caddy never
# receives their traffic; nothing to configure here.
